<!DOCTYPE html>
<html>
<head>
  <title><%= @topic.title %> - FriendChan</title>
</head>
<body>
  <div class="nav">
    <h1><%= @topic.title %></h1>
    <a href="/">[Back to Topics]</a>
  </div>
  
  <div class="topic">
    <p><%= @topic.content %></p>
    <% if @topic.image_path %>
      <img src="<%= @topic.image_path %>" alt="Topic image">
    <% end %>
    <small>Posted <%= @topic.created_at.strftime("%Y-%m-%d %H:%M") %></small>
  </div>
  
  <div class="posts">
    <% @topic.posts.each do |post| %>
      <div class="post">
        <p><%= post.content %></p>
        <% if post.image_path %>
          <img src="<%= post.image_path %>" alt="Post image">
        <% end %>
        <small>Posted <%= post.created_at.strftime("%Y-%m-%d %H:%M") %></small>
      </div>
    <% end %>
  </div>
  
  <div class="new-post">
    <h2>Create New Post</h2>
    <form action="/topics/<%= @topic.id %>/posts" method="post">
      <div>
        <label for="post_content">Content:</label>
        <textarea id="post_content" name="post[content]" placeholder="Write your reply here..."></textarea>
      </div>
      
      <div>
        <label for="post_image_path">Image URL:</label>
        <input type="text" id="post_image_path" name="post[image_path]" placeholder="https://example.com/image.jpg">
      </div>
      
      <button type="submit">[Create Post]</button>
    </form>
  </div>
</body>
</html> 